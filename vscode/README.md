# VS Code + CovaSyn

Requires the official MCP extension for VS Code.

Add to your `settings.json` (User or Workspace):

See [`settings.snippet.json`](./settings.snippet.json) for the relevant block to merge into your existing settings.

After saving, reload VS Code. The MCP panel will show CovaSyn under the configured servers.
