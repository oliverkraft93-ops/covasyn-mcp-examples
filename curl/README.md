# Direct HTTP / curl examples

If you don't want to use the MCP client and prefer raw HTTP, CovaSyn exposes JSON-RPC over MCP-streaming-HTTP at `https://api.covasyn.com/mcp`.

This is mainly useful for debugging or for integrating with environments where the MCP SDK isn't available.
